{
  "name": "ts-technology-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npx vite build",
    "start": "cd frontend && npm run preview"
  }
}
